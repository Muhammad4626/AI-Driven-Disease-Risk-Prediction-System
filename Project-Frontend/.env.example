# Frontend Environment Configuration
# Copy this file to .env and update values for your environment

# API Configuration
# Development (separate ports): http://localhost:8000
# Production EC2 (same domain via Nginx): leave empty for relative same-origin paths
# Alternative: https://core.bugdoubt.com (explicit domain)
REACT_APP_API_URL=
